## Summary

Describe the problem and the behavior changed.

## Verification

- [ ] `npm run check`
- [ ] `npm run benchmark` when verification semantics or baselines changed

## Protocol impact

Note any change to requirement semantics, missing-evidence behavior, or
verification-record digests. Write `None` when the protocol is unchanged.
